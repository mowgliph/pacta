/**
 * Carpeta de Recursos Estáticos (Assets)
 *
 * Esta carpeta está destinada a contener todos los recursos estáticos
 * utilizados por la aplicación, especialmente aquellos necesarios durante
 * el proceso de compilación y empaquetado con electron-builder.
 *
 * Contenido típico:
 * - Iconos de la aplicación en diferentes formatos (.ico, .icns, .png).
 * - Logo de la aplicación.
 * - Imágenes, fuentes u otros archivos multimedia que se empaquetarán
 *   con la aplicación o se usarán en el proceso de compilación.
 *
 * Referencia en `package.json` (`build.directories.buildResources`):
 * La configuración de electron-builder en `package.json` apunta a esta
 * carpeta como el directorio de recursos de compilación.
 *
 * Ejemplo de iconos esperados (según package.json):
 * - assets/icon.ico (para Windows)
 * - assets/icon.icns (para macOS)
 * - Iconos .png de diferentes tamaños (para Linux AppImage, ej. 256x256.png)
 */

// Este archivo es puramente informativo y no necesita exportar nada.
// Puedes eliminarlo si lo prefieres, pero sirve como documentación.